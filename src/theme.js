// Keep these in sync with the inline bootstrap script in index.html, which
// applies the saved theme before Vue mounts to avoid a flash of the wrong theme.
export const THEME_STORAGE_KEY = 'site-theme'
export const DEFAULT_THEME = 'ff'
export const VALID_THEMES = ['ff', 'teal', 'solarpunk']
